server:
  port: 6065

spring:
  application:
    name: ms-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/EnGeL-322/Tienda-DEMO-Terminado.git # repositorio
          searchPaths:
            - config-data
          default-label: main

  security:
      name: root
      password: 123456